## 1. 架构设计

```mermaid
graph TB
    subgraph "前端层"
        A["React 18 SPA"]
        A1["组件层：状态墙/处置台/交接班"]
        A2["状态管理：React useState/useReducer"]
        A3["样式层：TailwindCSS 3 + 自定义 CSS 动画"]
    end
    subgraph "数据层"
        B["Mock 数据模块"]
        B1["车辆静态数据"]
        B2["实时温度/位置模拟数据"]
        B3["告警与处置记录数据"]
    end
    A --> A1 --> A2
    A2 --> B
    A3 --> A1
```

## 2. 技术描述

- **前端框架**：React@18 + TypeScript
- **构建工具**：Vite@5
- **样式方案**：TailwindCSS@3 + 自定义 CSS 变量 + 原生 CSS 动画（关键帧）
- **状态管理**：React Hooks（useState / useReducer / useEffect），不引入额外状态库
- **图标方案**：lucide-react 线性图标库
- **字体方案**：Google Fonts - JetBrains Mono（数字数据） + Noto Sans SC（中文）
- **后端**：无后端，前端内置 Mock 数据模拟实时更新
- **数据持久化**：localStorage 存储交接班记录与告警处置状态

## 3. 路由定义

| Route | 页面用途 |
|-------|----------|
| `/` | 主看板页面，三栏布局展示全部模块 |

单页应用，无路由跳转，所有模块在同一页面通过 Tab/折叠切换可见性。

## 4. 数据模型

### 4.1 数据实体定义

```mermaid
erDiagram
    VEHICLE {
        string id PK "车辆ID"
        string plateNumber "车牌号"
        string driverName "司机姓名"
        string driverPhone "司机电话"
        string route "线路"
        string cargoOwner "货主"
        string tempZone "温区要求(冷冻/冷藏/恒温)"
        string batchNo "货品批次"
        float currentTemp "当前温度"
        float targetTemp "目标温度"
        string currentLocation "当前位置"
        string status "状态(normal/warning/poweroff)"
        int powerOffMinutes "断电持续分钟数"
        datetime lastUpdate "最后更新时间"
    }

    ALERT {
        string id PK "告警ID"
        string vehicleId FK "关联车辆ID"
        string level "告警级别(critical/warning)"
        string type "告警类型(poweroff/temp_high/device_error)"
        datetime startTime "告警开始时间"
        string status "状态(open/processing/closed/handover)"
        string assignee "处置值班员"
    }

    DISPOSAL_STEP {
        string id PK "步骤ID"
        string alertId FK "关联告警ID"
        int stepOrder "步骤序号(1/2/3)"
        string stepName "步骤名称"
        string action "操作动作"
        datetime actionTime "操作时间"
        string result "联系结果"
        string remark "备注"
        string operator "操作人"
    }

    HANDOVER_LOG {
        string id PK "记录ID"
        datetime handoverTime "交接班时间"
        string fromOperator "交班人"
        string toOperator "接班人"
        json openAlerts "未关闭告警列表"
        json recoveredVehicles "已恢复车辆列表"
        json cargoRisks "货损风险列表"
        string summary "交班摘要"
    }

    VEHICLE ||--o{ ALERT : has
    ALERT ||--o{ DISPOSAL_STEP : has
```

### 4.2 TypeScript 类型定义

```typescript
type VehicleStatus = 'normal' | 'warning' | 'poweroff';
type TempZone = 'frozen' | 'chilled' | 'constant';
type AlertLevel = 'critical' | 'warning';
type AlertType = 'poweroff' | 'temp_high' | 'device_error';
type AlertStatus = 'open' | 'processing' | 'closed' | 'handover';

interface Vehicle {
  id: string;
  plateNumber: string;
  driverName: string;
  driverPhone: string;
  route: string;
  cargoOwner: string;
  tempZone: TempZone;
  batchNo: string;
  currentTemp: number;
  targetTemp: number;
  currentLocation: string;
  status: VehicleStatus;
  powerOffMinutes: number;
  lastUpdate: Date;
  tempHistory: number[];
}

interface DisposalStep {
  id: string;
  stepOrder: 1 | 2 | 3;
  stepName: string;
  action: string;
  actionTime: Date | null;
  result: string;
  remark: string;
  operator: string;
  completed: boolean;
}

interface Alert {
  id: string;
  vehicleId: string;
  level: AlertLevel;
  type: AlertType;
  startTime: Date;
  status: AlertStatus;
  assignee: string;
  steps: DisposalStep[];
}

interface HandoverLog {
  id: string;
  handoverTime: Date;
  fromOperator: string;
  toOperator: string;
  openAlerts: string[];
  recoveredVehicles: string[];
  cargoRisks: { vehicleId: string; description: string; level: string }[];
  summary: string;
  confirmed: boolean;
}
```
